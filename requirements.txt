# --- Core Web Framework & Server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-dotenv>=1.0.1
python-multipart>=0.0.12
aiofiles>=24.1.0

# --- Database (PostgreSQL) ---
psycopg2-binary>=2.9.9

# --- Vector Database (ChromaDB) ---
chromadb>=0.5.15

# --- Google Gemini AI SDK ---
google-genai>=2.17.0

# --- Embeddings & ML Models ---
sentence-transformers>=3.1.1
torch>=2.4.1

# --- Document Parsing & Processing (marker-master) ---
# marker-master dùng trực tiếp từ thư mục marker-master/ (sys.path)
# Dependencies của marker-master:
weasyprint>=61.0        # Chuyển DOCX/HTML → PDF (DocumentProvider)
mammoth>=1.4.0          # Chuyển DOCX → HTML (DocumentProvider)
python-pptx>=1.0.0      # Đọc PPTX (PowerPointProvider)
openpyxl>=3.1.0         # Đọc XLSX (SpreadSheetProvider)

# --- Vietnamese NLP & Preprocessing ---
underthesea>=9.5.0

# --- Data Utilities & Helpers ---
numpy>=1.26.4
pydantic>=2.9.2
httpx>=0.27.2
pillow>=10.4.0,<11.0.0

# --- Sparse Retrieval (BM25 + RRF Hybrid) ---
rank_bm25>=0.2.2          # BM25 sparse retrieval, kết hợp với dense ChromaDB qua RRF