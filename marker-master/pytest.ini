[pytest]
testpaths=tests
markers =
    filename(name): specify the filename for the pdf_document fixture
    integration: end-to-end quality checks on real pages (slow; runs the VLM)
    cpu: runs without a GPU or inference server (config, providers); also run on the CPU CI runner
filterwarnings =
    ignore::Warning